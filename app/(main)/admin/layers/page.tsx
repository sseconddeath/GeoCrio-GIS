import { StagePlaceholder } from '@/components/ui/StagePlaceholder';

export default function AdminLayersPage() {
  return (
    <StagePlaceholder
      stage={6}
      title="Слои карты"
      description="Загрузка растровых и векторных слоёв (GeoTIFF/GeoJSON) появится на Этапе 6."
    />
  );
}

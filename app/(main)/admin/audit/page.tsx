import { StagePlaceholder } from '@/components/ui/StagePlaceholder';

export default function AdminAuditPage() {
  return (
    <StagePlaceholder
      stage={6}
      title="Аудит-лог"
      description="Просмотр журнала изменений (audit_log) появится на Этапе 6."
    />
  );
}

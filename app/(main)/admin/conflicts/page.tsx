import { StagePlaceholder } from '@/components/ui/StagePlaceholder';

export default function AdminConflictsPage() {
  return (
    <StagePlaceholder
      stage={6}
      title="Конфликты синхронизации"
      description="Разрешение конфликтов офлайн-синхронизации (sync_conflicts) появится на Этапе 6."
    />
  );
}

# ГеоКрио ГИС (GeoCrio GIS)

Веб-ГИС + мобильное PWA для геоинформационного обеспечения района учебных
полевых практик и геокриологического полигона. Полное ТЗ — см. `docs/`.

## Статус

Этап 0 (схема БД) — готов и проверен запуском на локальном Postgres 16 +
PostGIS 3 (регистрация → автосоздание профиля, RLS по ролям, триггер
"точка вне полигона", запрет самоповышения роли, аудит insert/update/
delete/restore). Подробности и найденные при этом баги — в
`docs/schema-review.md`. Дальше — по плану ниже.

## Этапы разработки

Порядок выбран так, чтобы каждый этап был проверяемым сам по себе и не
блокировался незавершённостью следующего.

- **Этап 0 — Схема БД (готово)**
  `supabase/migrations/001_initial_schema.sql`, `002_storage_setup.sql`.
  Таблицы, RLS, триггеры (аудит, автосоздание профиля, запрет самоповышения
  роли), views, Storage-политики. См. `docs/schema-review.md` — что было
  исправлено относительно исходного ТЗ.

- **Этап 1 — Каркас Next.js + Auth**
  `create-next-app`, Tailwind, Supabase client/server, страницы
  `login`/`register`, чтение роли из `profiles`, защищённый layout
  `(main)`. Без этого нечего показывать на карте — доступ к данным идёт
  через RLS, значит нужна работающая сессия.

- **Этап 2 — Карта + CRUD скважин/точек наблюдений**
  MapLibre GL JS поверх OSM-тайлов, попапы, формы создания/редактирования
  `boreholes` и `observation_points`, soft delete. Первый реально
  проверяемый на полигоне функционал.

- **Этап 3 — Фото и Realtime**
  Загрузка фото (сжатие на клиенте, EXIF/GPS), подписка на
  `postgres_changes` для live-обновления маркеров у нескольких
  одновременно работающих пользователей.

- **Этап 4 — Офлайн-режим (PWA)**
  `next-pwa`, Service Worker, IndexedDB (`sync_queue`, `cached_data`,
  `tile_cache`), менеджер синхронизации, мобильный UI. Самый рискованный
  этап архитектурно — сознательно идёт после того, как онлайн-версия уже
  проверена на реальных данных.

- **Этап 5 — Замеры, аналитика, экспорт**
  Time-series замеров, температурный профиль (график), выгрузка
  растровых слоёв из QGIS, экспорт GeoJSON/CSV/Shapefile.

- **Этап 6 — Админка**
  Разрешение конфликтов синхронизации, аудит-лог, управление
  пользователями, полировка UI.

## Локальный запуск миграций

```bash
supabase link --project-ref <project-ref>
supabase db push
```

или через SQL Editor в Supabase Dashboard — накатить файлы из
`supabase/migrations/` по порядку.

## Переменные окружения

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY` (только серверные API routes)
